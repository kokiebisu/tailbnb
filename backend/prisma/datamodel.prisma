type Stay {
  id: ID! @id
  title: String! @unique
  cost: Int!
  hostType: String
  country: String
  location: String
  ratings: Float @default(value: 0.0)
}

type Adventure {
  id: ID! @id
  title: String
  period: Int!
  cost: Int!
  country: String!
}
